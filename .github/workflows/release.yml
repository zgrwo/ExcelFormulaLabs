name: Release

on:
  push:
    tags:
      - 'v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write

jobs:
  build-test:
    name: Build & Test (Release)
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x

      - name: Restore
        run: dotnet restore

      - name: Build (Release)
        run: dotnet build -c Release --no-restore

      - name: Test (Release, all TFMs)
        run: dotnet test -c Release --no-build --verbosity normal

  verify-docs:
    name: Verify Docs
    runs-on: ubuntu-latest
    needs: build-test
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Run verify-docs.sh
        run: bash scripts/verify-docs.sh

  cross-val:
    name: Cross-Validation
    runs-on: windows-latest
    needs: build-test
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 8.0.x

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install numpy scipy scikit-learn

      - name: Build CrossValRunner
        run: dotnet build tests/CrossValRunner/CrossValRunner.csproj -c Release

      - name: Run verify-manual.py
        run: python scripts/verify-manual.py

  pack-release:
    name: Pack & Release
    runs-on: windows-latest
    needs: [cross-val, verify-docs]
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x

      - name: Extract version from tag
        id: version
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $version = $tag -replace '^v', ''
          echo "VERSION=$version" >> $env:GITHUB_OUTPUT

      - name: Verify version consistency (tag == Directory.Build.props)
        shell: pwsh
        run: |
          $props = [xml](Get-Content src/Directory.Build.props)
          $propsVersion = $props.Project.PropertyGroup.Version
          if ("${{ steps.version.outputs.VERSION }}" -ne $propsVersion) {
            Write-Error "Tag version ${{ steps.version.outputs.VERSION }} does not match Directory.Build.props <Version>$propsVersion</Version>. Bump src/Directory.Build.props before tagging."
            exit 1
          }
          Write-Host "Version OK: $propsVersion"

      - name: Build (Release)
        run: dotnet build -c Release

      - name: Collect XLL artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifacts
          Get-ChildItem -Recurse -Filter "*-packed.xll" -Path "src" |
            Where-Object { $_.DirectoryName -match "Release" } |
            Copy-Item -Destination artifacts/
          Get-ChildItem artifacts/

      - name: Pack NuGet (Foundation)
        run: dotnet pack src/Foundation/Foundation.csproj -c Release --no-build -o artifacts/nupkg

      - name: Push to GitHub Packages
        shell: pwsh
        run: |
          Get-ChildItem "artifacts/nupkg/*.nupkg" | ForEach-Object {
            dotnet nuget push $_.FullName --source "github" --api-key "${{ secrets.GITHUB_TOKEN }}" --skip-duplicate
          }

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          name: "ExcelFormulaLabs ${{ github.ref_name }}"
          body: |
            ## ExcelFormulaLabs ${{ github.ref_name }}

            ### 安装
            下载对应 `.xll` 文件，在 Excel 中加载即可使用。

            | 文件 | 框架 | 说明 |
            |------|------|------|
            | Analytics-AddIn-net48-*-packed.xll | .NET Framework 4.8 | 免安装运行时 |
            | Analytics-AddIn-net8.0-*-packed.xll | .NET 8 | 性能更优 |
            | DataToolkit-AddIn-net48-*-packed.xll | .NET Framework 4.8 | 免安装运行时 |
            | DataToolkit-AddIn-net8.0-*-packed.xll | .NET 8 | 性能更优 |

            详见 [CHANGELOG](CHANGELOG.md)。
          files: artifacts/*.xll
          draft: false
          prerelease: false